[project]
name = "child-control-server"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["fastapi", "uvicorn"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
include = ["app.py", "db.py", "add_device.py"]
